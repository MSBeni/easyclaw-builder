# Public-release gates

This records the checks for the first public source release. Publishing an npm package or container image is a separate operator decision; CI does neither. The public repository remains experimental and CLI-first, with a preview-only native Builder page.

| Gate | Current evidence | Status |
| --- | --- | --- |
| Fresh history and isolated repository | New history; noreply commit identity; old fork not copied | Complete |
| Runtime compatibility | Pinned OpenClaw v2026.9.4; plugin build/validate and isolated runtime load; two generated Claws validate and `claws dev` preview | Automated checks complete |
| Product workflow | Local on-demand CLI path creates an editable plan, packages a read-only Claw, previews OpenClaw's exact add plan, requires its integrity digest, installs, and checks status in isolated state | Local CLI slice complete; native Builder page remains preview-only |
| Live host smoke | Linked plugin in an isolated OpenClaw v2026.9.4 Gateway on macOS and again in a clean Linux Node 24.19 container without a mounted home or credentials. On Linux, the native page loaded all six templates, selected Research Agent from a single-role brief, and rendered a Claw preview with manual setup tasks. The clean Linux container also passed the CLI install/status walkthrough. | CLI and native preview pass across macOS and isolated Linux; no second physical machine tested |
| Tests and CI | A fresh clone with frozen-lockfile pnpm 10.23 and Node 24.19 passed typecheck, 28 unit tests, build with a clean Git diff, plugin validation, template smoke, and the local install smoke. The source-history audit passed. [Pre-publication CI on protected `main`](https://github.com/MSBeni/easyclaw-builder/actions/runs/35263007197) passed, including 30 unit tests and the dependency audit. | Technical checks pass; rerun after future code changes |
| Source and license audit | All reachable blobs and commit emails scanned; copied-module provenance reviewed; the MIT notice from the copied OpenClaw version is retained alongside the EasyClaw contributor line. The maintainer selected MIT for this source project. | Technical review complete; not a legal opinion |
| Public support scope | Experimental, CLI-first source project pinned to OpenClaw v2026.9.4; the native Builder page is preview-only, with no package release or production-support promise | Decision recorded in README |
| Dependency audit | Frozen-lockfile clean clone and protected `main` CI returned no known moderate-or-higher vulnerabilities | Passed at publication |
| Documentation | README covers agent use, active config path, symlinked-state recovery, disposable state, partial-install recovery, and Gateway-dependent previewed removal. A new clone on macOS and a clean Linux container each completed frozen install, build, plan, package, preview, consent-bound install, and healthy status. The native preview also passed in the clean Linux container. The earlier isolated authenticated Gateway removal passed. | CLI and native preview pass in isolated environments; model-backed local turn passes; no second physical-machine walkthrough |
| Repository safeguards | After publication, GitHub API read-back confirmed `main` requires a PR, up-to-date `verify` CI, resolved conversations, and linear history, enforced for admins; force-push and deletion are blocked. Actions has read-only default permissions and cannot approve PRs. Dependabot alerts and security-update PRs, secret scanning with push protection, private vulnerability reporting, and first-time fork-contributor workflow approval are enabled. The 18 pre-publication Actions logs present on September 17 were scanned for common credential and personal-path patterns without matches. | Public settings verified; no CI job monitors future settings drift |

The native template preview is not a promise that a generated agent will perform the old Blueprint's schedules, channel replies, or deliveries. Those settings are listed as manual setup tasks and are not silently installed. The local CLI path is separate: it creates only a new read-only, on-demand agent. Its automated isolated smoke test verifies installation and status, but does not run a model, connect an account or channel, or create a cron job. The separate manual model-backed acceptance below covers one local turn.

Removal requires a running authenticated Gateway. It completed against an isolated loopback Gateway on pinned OpenClaw v2026.9.4: the install record and workspace were removed. The README still documents partial-removal recovery; this test does not prove every host configuration. If removal becomes a supported product workflow, add an automated live-Gateway removal smoke. The two TypeBox packages serve different existing surfaces (migrated Blueprint code and the OpenClaw feature contract). Packaging metadata such as `peerDependencies` and npm `files` remains out of scope while this repository is source-only; revisit before any package distribution.

On September 17, the maintainer's symlinked `~/.openclaw` state prevented atomic config repair. Using the real state path, the pinned OpenClaw doctor completed legacy migrations and OpenAI OAuth sign-in succeeded. The old Google and Anthropic keys were rejected by their providers and were not used for acceptance. In a separate disposable state with its own short-lived OAuth login, Builder generated and installed a read-only Claw with no channel or schedule. Its `agent --local` turn read `NOTE.txt` and correctly cited the file using `openai/gpt-5.6-sol` and the OpenClaw harness; a second fresh session also passed without the explicit runtime override. Neither turn used `--deliver`. The disposable state, credential, package, and workspace were removed. A separate `agent exec` probe of the Codex app-server harness failed during cleanup on this Mac; that one-shot harness path is not the Builder `agent --local` path and remains unverified. Other models, providers, and physical hosts are not covered by this result.

## Source review scope

The fresh history contains only the small Builder source tree, documentation, lockfile, and verification workflow. The copied portable Blueprint modules and approval-posture logic are identified in `NOTICE.md`; the OpenClaw MIT copyright and permission notice remain in `LICENSE`. The history scan checked every reachable Git blob for listed secret and personal-path patterns and checked commit email metadata. Manual inspection found no tracked media, signing material, runtime config, personal briefs, or bundled dependency code. This is a technical redistribution review, not a legal opinion or a guarantee that pattern scanning catches every secret.

## Publication verification

The repository became public after the protected `main` run passed on September 17, 2026. The visibility-change API response showed secret scanning and push protection enabled; subsequent API reads confirmed both. Private vulnerability reporting was enabled so `SECURITY.md` has a working destination, and fork PR workflow runs require approval for first-time contributors. GitHub API reads also reconfirmed `main` branch protection, Dependabot alerts and security-update PRs, and read-only Actions permissions after publication. The log scan is pattern-based, not a guarantee that every sensitive value was found. No CI job verifies repository settings or detects future drift.

## Compatibility policy

Pin the tested OpenClaw host version and its Node runtime. When OpenClaw releases a new version, update the pin in `package.json`, rebuild the browser assets, rerun plugin validation and Claw validation, and perform one manual Gateway/UI smoke test before claiming support. Feature-plugin and Claw interfaces are experimental, so compatibility is demonstrated by tests rather than inferred from a semver range.
