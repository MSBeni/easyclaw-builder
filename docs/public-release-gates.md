# Public-release gates

Use this checklist before changing repository visibility. Publishing source is a separate operator decision; CI does not publish npm packages or container images. A public repository would still be experimental and CLI-first, with a preview-only native Builder page.

| Gate | Current evidence | Status |
| --- | --- | --- |
| Fresh history and isolated repository | New history; noreply commit identity; old fork not copied | Complete |
| Runtime compatibility | Pinned OpenClaw v2026.9.4; plugin build/validate and isolated runtime load; two generated Claws validate and `claws dev` preview | Automated checks complete |
| Product workflow | Local on-demand CLI path creates an editable plan, packages a read-only Claw, previews OpenClaw's exact add plan, requires its integrity digest, installs, and checks status in isolated state | Local CLI slice complete; native Builder page remains preview-only |
| Live host smoke | Linked plugin in an isolated, loopback-only OpenClaw v2026.9.4 Gateway; native page loaded six templates, suggested a template from a brief, and rendered a Claw preview with manual setup tasks. A clean Linux Node 24.19 container separately passed the CLI install/status walkthrough without mounted home or credentials. | CLI passes across macOS and Linux; repeat the native UI on a separate clean host before publication |
| Tests and CI | A fresh clone with frozen-lockfile pnpm 10.23 and Node 24.19 passed typecheck, 28 unit tests, build with a clean Git diff, plugin validation, template smoke, and the local install smoke. The source-history audit passed. [Hosted CI on `main`](https://github.com/MSBeni/easyclaw-builder/actions/runs/35239417890) passed after the release-evidence update. | Technical checks pass; rerun after future code changes |
| Source and license audit | All reachable blobs and commit emails scanned; copied-module provenance reviewed; the MIT notice from the copied OpenClaw version is retained alongside the EasyClaw contributor line. The maintainer selected MIT for this source project. | Technical review complete; not a legal opinion |
| Public support scope | Experimental, CLI-first source project pinned to OpenClaw v2026.9.4; the native Builder page is preview-only, with no package release or production-support promise | Decision recorded in README |
| Dependency audit | Frozen-lockfile clean clone returned no known moderate-or-higher vulnerabilities | Local pass; rerun at publication time |
| Documentation | README covers agent use, active config path, disposable state, partial-install recovery, and Gateway-dependent previewed removal. A new clone on macOS and a clean Linux container each completed frozen install, build, plan, package, preview, consent-bound install, and healthy status. The earlier isolated authenticated Gateway removal also passed. | Manual CLI flow passes on two environments; no successful model-backed turn or separate-host native UI walkthrough yet |
| Repository safeguards | `main` requires a PR, up-to-date `verify` CI from GitHub Actions, resolved conversations, and linear history, enforced for admins; force-push and deletion are blocked. Actions has read-only default permissions. Dependabot alerts and security-update PRs are enabled. The 18 Actions logs present on September 17 were scanned for common credential and personal-path patterns without matches. | Verified while private; public-only settings need recheck at visibility change |

The native template preview is not a promise that a generated agent will perform the old Blueprint's schedules, channel replies, or deliveries. Those settings are listed as manual setup tasks and are not silently installed. The local CLI path is separate: it creates only a new read-only, on-demand agent. Its isolated smoke test verifies installation and status, but does not run a model, connect an account or channel, or create a cron job.

Removal requires a running authenticated Gateway. It completed against an isolated loopback Gateway on pinned OpenClaw v2026.9.4: the install record and workspace were removed. The README still documents partial-removal recovery; this test does not prove every host configuration. If removal becomes a supported product workflow, add an automated live-Gateway removal smoke. The two TypeBox packages serve different existing surfaces (migrated Blueprint code and the OpenClaw feature contract). Packaging metadata such as `peerDependencies` and npm `files` remains out of scope while this repository is source-only; revisit before any package distribution.

An isolated model-backed acceptance turn was attempted after the fresh-clone install but did not succeed: the available local model credentials were invalid or incompatible with this pinned OpenClaw version. No model reply was verified. The temporary credential copy and the entire disposable checkout and state were removed after the test. A current provider login is needed before rerunning the undelivered local turn from the README.

## Source review scope

The fresh history contains only the small Builder source tree, documentation, lockfile, and verification workflow. The copied portable Blueprint modules and approval-posture logic are identified in `NOTICE.md`; the OpenClaw MIT copyright and permission notice remain in `LICENSE`. The history scan checked every reachable Git blob for listed secret and personal-path patterns and checked commit email metadata. Manual inspection found no tracked media, signing material, runtime config, personal briefs, or bundled dependency code. This is a technical redistribution review, not a legal opinion or a guarantee that pattern scanning catches every secret.

## Visibility-change checklist

At this verification the repository remained private. MIT attribution and the experimental, CLI-first source scope are recorded above. GitHub did not offer private vulnerability reporting or first-time public fork-PR approval while private, and rejected secret scanning for this private personal repository. If visibility is changed, immediately enable private vulnerability reporting so `SECURITY.md` has a working destination, secret scanning with push protection, and approval for first-time contributors' workflow runs. Recheck the `main` rule, Dependabot, and read-only Actions permissions after the change rather than assuming private-repository rules carry over. The log scan is pattern-based, not a guarantee that every sensitive value was found. No CI job verifies repository settings.

## Compatibility policy

Pin the tested OpenClaw host version and its Node runtime. When OpenClaw releases a new version, update the pin in `package.json`, rebuild the browser assets, rerun plugin validation and Claw validation, and perform one manual Gateway/UI smoke test before claiming support. Feature-plugin and Claw interfaces are experimental, so compatibility is demonstrated by tests rather than inferred from a semver range.
